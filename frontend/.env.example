# ── BaseballOS frontend environment ─────────────────────────────────────────
# Copy to `.env` only if you need to override the API location:
#     cp .env.example .env
#
# Base ORIGIN of the backend API (no trailing /api — the app appends it).
# Leave this UNSET for local development: the Vite dev server proxies /api to
# http://127.0.0.1:5000 (see vite.config.js), so no value is needed.
# Set it only when the backend is hosted separately (e.g. production builds):
#     VITE_API_BASE_URL=https://your-backend.example.com
VITE_API_BASE_URL=

# Admin token for the operator-only "Recalculate" / sync actions, sent as the
# X-Admin-Token header. OPTIONAL and usually left UNSET: anything set here is
# baked into the public JS bundle, so only use it for a private/internal build
# pointed at a token-protected backend. For a public demo, leave it blank and
# trigger protected endpoints with curl instead (see docs/SETUP.md).
# VITE_ADMIN_API_TOKEN=
